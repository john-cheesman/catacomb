{
  "name": "catacomb",
  "description": "Descend through the maze...",
  "author": "John Cheesman",
  "version": "1.0.0",
  "devDependencies": {
    "babel-core": "^6.18.2",
    "babel-preset-es2015": "^6.18.0",
    "babel-register": "^6.18.0",
    "browserify": "^13.1.1",
    "cpy-cli": "^1.0.1",
    "env-cmd": "^2.2.0",
    "http-server": "^0.9.0",
    "mkdirp": "^0.5.1",
    "rimraf": "^2.5.4",
    "runjs": "^2.5.1",
    "tsify": "^2.0.2",
    "typescript": "^2.0.10",
    "uglifyify": "^3.0.4",
    "watch": "^1.0.1"
  },
  "dependencies": {
    "phaser": "^2.6.2"
  },
  "scripts": {
    "build": "env-cmd .env run build",
    "serve": "env-cmd .env run serve",
    "watch": "env-cmd .env run watch",
    "start": "env-cmd .env run start"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}

